include:
    - backend/compose.yaml

services:
    frontend:
        build:
            context: ./frontend
        ports:
            - '5174:5173'
        volumes:
            - ./frontend:/app
            - /app/node_modules
        environment:
            - VITE_API_URL=http://localhost
        networks:
            - sail
